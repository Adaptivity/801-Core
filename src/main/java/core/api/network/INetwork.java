package core.api.network;

/**
 * An interface of all existing networks.
 * <p>
 *     A network should never be static or the same through-out it's entire life-cycle.
 * </p>
 * @author Master801
 */
public interface INetwork {
}
